// 34. Create a function that returns an array with words inside a text.

function wordsOfText(str) {
    let arr = [];
    arr.push(str)
    console.log(arr);
    
    console.log([arr.length]);
}

wordsOfText('naman');